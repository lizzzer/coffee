*Mitä onkaan tapahtunut*
  <% _.each(json.history, function(machine) {%><%=machine.history_message%> <%= moment(machine.createdAt).format( 'MMM-DD: HH:mm')%>. Thanks to: *<%=machine.user%>*
  <% }) %>
